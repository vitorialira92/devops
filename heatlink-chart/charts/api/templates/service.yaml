apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.global.api.name }}
  labels:
    run: {{ .Values.global.api.name }}
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: {{ .Values.global.api.name }}
