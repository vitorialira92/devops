apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.global.postgres.name }}
  labels:
    run: {{ .Values.global.postgres.name }}
spec:
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: {{ .Values.global.postgres.name }}
