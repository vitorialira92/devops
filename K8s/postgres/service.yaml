apiVersion: v1
kind: Service
metadata:
  name: heatlink-postgres
  labels:
    run: heatlink-postgres
spec:
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: heatlink-postgres
