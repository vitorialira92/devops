apiVersion: v1
kind: Service
metadata:
  name: heatlink-redis
  labels:
    run: heatlink-redis
spec:
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
  selector:
    app: heatlink-redis
